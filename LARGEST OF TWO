#include <stdio.h>

int main()
{
    int a,b;
    printf("Enter the value for two numbers\n");
    scanf("%d%d",&a,&b);
    if(a>b)
      printf("THE LARGE NUMBER IS : %d",a);
     else
     printf("THE LARGE NUMBER IS : %d",b);
}
