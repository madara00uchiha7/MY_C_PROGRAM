#include <stdio.h>  
int largest(int a, int b) {
    if (a > b)
        return a;
    else
        return b;
}
int main() {
    int x, y, max;

    printf("Enter two numbers: ");
    scanf("%d %d", &x, &y);

    max = largest(x,y);

    printf("The largest number is: %d\n", max);
}
    
