#include <stdio.h>

int main()
{ 
    float radius,area;
    const float pi=3.14;
    printf("Enter the Radius of the circle\n");
    scanf("%f",&radius);
    area=pi*radius*radius;
    printf("The Area of the circle is : %.2f\n",area);
}
