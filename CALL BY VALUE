#include <stdio.h>
#include<string.h>
int main() {
    int x,y,temp;
    printf("Enter the value of x,y\n");
    scanf("%d%d",&x,&y);
    temp=x;
    x=y;   //call by value
    y=temp;
    printf("The swap value is %d and %d",x,y);
}
