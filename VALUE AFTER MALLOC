#include <stdio.h>
#include <stdlib.h>
int main() {
    int *p;
    p = (int*)malloc(sizeof(int));
    printf("Enter the value of p: ");
    scanf("%d", p);  
    printf("Value is %d\n", *p);
    free(p);
   printf("Value after free: %d\n", p);
   
}
