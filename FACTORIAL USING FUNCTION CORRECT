#include<stdio.h>
int factorial(int n) {
    if (n > 1) 
        return n*factorial(n-1);
}
int main() {
    int n,fact;
    printf("ENTER A NUMBER: ");
    scanf("%d", &n);
    fact=factorial(n);   
    printf("THE FACTORIAL IS %lld\n", fact);
}

