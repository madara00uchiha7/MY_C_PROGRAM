#include <stdio.h>
#include <string.h>

int main() {
    char src[100]; 
    char dest[100];
    printf("Enter the name\n");
    fgets(src,sizeof(src),stdin);
    strcpy(dest, src);
   printf("Source string: %s\n", src);
    printf("Copied string: %s\n", dest);
}
